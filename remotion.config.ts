import {Config} from 'remotion';

export const config = {
  fps: 60,
  width: 1920,
  height: 1080,
  previewPort: 3001,
  jpegQuality: 95,
  videoBitrate: '8000k',
  logLevel: 'verbose',
};

export default config;
